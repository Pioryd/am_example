{
  "required": [
    "characters",
    "database_scripts",
    "world_client",
    "admin_server"
  ],
  "properties": {
    "characters": {
      "type": "object",
      "required": ["included"],
      "properties": {
        "included": { "type": "array" },
        "excluded": { "type": "array" }
      }
    },
    "database_scripts": {
      "type": "object",
      "required": ["url", "name"],
      "properties": {
        "url": { "type": "string" },
        "name": { "type": "string" }
      }
    },
    "api_client": {
      "type": "object",
      "required": ["login", "password", "options", "socket_io_options"],
      "properties": {
        "login": { "type": "string" },
        "password": { "type": "string" },
        "options": { "type": "object" },
        "socket_io_options": { "type": "object" }
      }
    },
    "world_client": {
      "type": "object",
      "required": ["login", "password", "options", "socket_io_options"],
      "properties": {
        "login": { "type": "string" },
        "password": { "type": "string" },
        "options": { "type": "object" },
        "socket_io_options": { "type": "object" }
      }
    },
    "admin_server": {
      "type": "object",
      "required": ["login", "password", "options", "socket_io_options"],
      "properties": {
        "login": { "type": "string" },
        "password": { "type": "string" },
        "options": { "type": "object" },
        "socket_io_options": { "type": "object" }
      }
    }
  }
}
