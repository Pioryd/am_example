{
  "required": ["database", "world_server", "admin_server", "am_data_rules"],
  "properties": {
    "database": {
      "type": "object",
      "required": ["url", "name"],
      "properties": {
        "url": { "type": "string" },
        "name": { "type": "string" }
      }
    },
    "world_server": {
      "type": "object",
      "required": ["options", "socket_io_options"],
      "properties": {
        "options": { "type": "object" },
        "socket_io_options": { "type": "object" }
      }
    },
    "admin_server": {
      "type": "object",
      "required": ["login", "password", "options", "socket_io_options"],
      "properties": {
        "login": { "type": "string" },
        "password": { "type": "string" },
        "options": { "type": "object" },
        "socket_io_options": { "type": "object" }
      }
    },
    "am_data_rules": {
      "type": "object"
    }
  }
}
